export const environment = {
  production: true,
  // This will be replaced with the actual Railway URL during deployment
  // For Railway, the API URL will be the same as the frontend URL since we serve from backend
  apiUrl: '/api'
};
